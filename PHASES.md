# Signal Scout — build phases (live checklist)

Tick `[ ]` → `[x]` only when the phase's checkpoint passes AND it's committed.
Build in order. On a block: make the reasonable assumption, log it below, stub behind a `TODO`, keep going.

- [x] **Phase 0 — Scaffold.** Next.js 15 + TS + Tailwind + ui primitives; `docker-compose.yml` (Postgres+pgvector, optional SearXNG); Drizzle configured; `.env.example`; scripts. **Checkpoint:** `pnpm dev` boots (HTTP 200 ✓); `pnpm db:push` creates schema (20 tables ✓); pgvector enabled ✓.
- [x] **Phase 1 — Schema + auth.** All BUILD_PLAN §4 tables in Drizzle + pgvector HNSW + pg_trgm. Auth.js v5 (GitHub OAuth + magic link + dev sign-in); first login creates org+user. **Checkpoint:** real login flow ✓ → org `Ada's workspace` + user `founder@acme.io` (owner) + audit log created ✓; typecheck ✓; tests ✓.
- [x] **Phase 2 — Source adapters (free).** SEC EDGAR (Form D, UA + rate cap), Greenhouse + Lever + Ashby (public JSON), GitHub (Octokit), web-diff (content hash), lu.ma. Each → normalized `RawItem[]` with cursors. **Checkpoint:** live smoke ✓ — github facebook/react, sec CIK 320193 (Apple), greenhouse stripe (505 jobs), lever spotify/palantir, ashby ramp (112), web stripe.com/blog; 13 fixture mapping tests + zod-valid; polite per-host rate limiting + SEC User-Agent.
- [x] **Phase 3 — Normalizer + entity resolution.** `normalizeDomain`/`normalizeName`/company-suffix/linkedin/email; company-by-domain (exact-name dedupe when domain-less), person-by-strong-key; no name-only auto-merge (suggestions logged to `audit_logs`); `content_hash` dedupe; upserts. **Checkpoint:** live verify ✓ — ingest twice = 3 then 0 dupes; two same-name no-strong-key people stay separate; same linkedin unifies. 12 unit tests + 30 total green.
- [x] **Phase 4 — ICPs + prefilter + classifier.** `/icps` CRUD (create/edit/delete/pause); embed ICP defs (local hash embedder, swappable); cosine prefilter; LLM classifier (Anthropic/Ollama via AI SDK, zod-validated, deterministic mock fallback) → `{type, strength, matched_icp_ids, justification}`; source-aware + hard signalType filter for precision; persist signals; log `llm_runs` (261 logged). **Checkpoint:** seeded ICP over 121 real signals → GTM roles=Expansion(0.76), dev releases matched(0.71), non-GTM hires excluded(0.23); 10 hand-checked correct. 39 tests green.
- [x] **Phase 5 — Eval harness.** `evals/golden/classification/*.json` (30 hand-labeled real items: ats/github/sec/web/events); `evals/run.ts` prints per-type precision/recall/F1 + strength gate and FAILS (exit 1) under thresholds (accuracy<0.8, per-type recall<0.6). **Checkpoint:** `pnpm eval` → 100% accuracy/30 items, exit 0; proven to exit 1 on a corrupted-label regression (funding recall 0.57<0.6).
- [x] **Phase 6 — Feed UI.** `/feed`: typed signal cards (per-type color/icon, strength meter, source badge, justification), sticky filter bar (ICP/type/source/min-strength/date), infinite scroll (IntersectionObserver + `/api/feed`), Deep-research + Add-to-list, toast system. Org-scoped via matched ICPs. **Checkpoint:** renders 51 real ICP-matched signals (screenshot verified); all filters narrow correctly incl. combined AND; distinctive beacon/radar brand.
- [x] **Phase 7 — Deep-research agent.** Tools: `web_search` (SearchProvider tavily/exa/searxng/mock), `fetch_page` (SSRF-guarded: blocks private/loopback/link-local/metadata IPs, non-http schemes, creds, unfollowed redirects; ToS-blocks LinkedIn/X), identity-verified `github_lookup`. Tool-call cap + cost ceiling + 30-min page cache + 7-day dossier cache. Citation guard (drop uncited, confidence=cited/total, low-confidence badge). LLM-synth + deterministic zero-key path. `/research` + `/people/[id]` panel w/ clickable sources + refresh. **Checkpoint:** Rauch dossier 100% cited (screenshot); common-name "Michael Chen" attributes nothing (low-confidence) — no mis-attribution. 13 SSRF/citation tests, 52 total green.
- [x] **Phase 8 — Lists + CSV + REST API.** Lists hold people + companies; `/lists` + `/lists/[id]` CRUD + members; feed Add-to-list wired to a "Saved" list; `GET /api/lists/:id/export.csv` (BOM + RFC4180). Hashed API keys (`/integrations` create/revoke) + dual auth (Bearer key or session). REST: `GET /api/signals`, `GET /api/people/:id`, `POST /api/people/:id/dossier`, `POST /api/research`. **Checkpoint:** CSV opens cleanly (BOM verified); REST scoped by key; invalid/no key → 401; research via key returns cited dossier. 53 tests green.
- [x] **Phase 9 — MCP server.** Shared tool layer + `mcp/` stdio (SDK `McpServer`, API-key auth) + `app/api/mcp` HTTP (JSON-RPC 2.0, Bearer). Tools: `search_signals`, `get_person`, `generate_dossier`, `list_signals_for_company`, `export_list`. `/integrations` copy-paste Claude Desktop/Cursor config (HTTP + local stdio). **Checkpoint:** real round-trip on BOTH transports — stdio `list_signals_for_company`→stripe+2 signals; HTTP `initialize`/`tools/list`/`search_signals`→3 signals; no key → 401. 55 tests green.
- [ ] **Phase 10 — Free tier + abuse control.** Per-user/org daily quotas on classify + research; Postgres token-bucket rate limiting (IP + user); dossier/classification caching; cheap model for classify; BYO-key flow (`/usage`); global budget kill-switch. **Checkpoint:** over-quota → clean 429 + BYO prompt; BYO key routes spend to the user.
- [ ] **Phase 11 — Multi-tenancy + security.** Every query org-scoped; RLS or enforced scoping with cross-tenant-read tests; hash API keys at rest; HMAC-sign + verify outbound webhooks; tighten SSRF; security headers/CSP; input-validation audit. **Checkpoint:** cross-tenant access fails closed; webhook signature verifies.
- [ ] **Phase 12 — Stage-2 delivery + accounts.** Signed webhooks on new high-strength matched signals; gated CRM push (confirm + audit, never auto); Slack notifications; daily/weekly email digest (Nodemailer); `/companies/[id]` timeline + org-tree; scheduled CSV→inbox/S3. **Checkpoint:** test webhook fires + verifies; digest renders; company view groups signals.
- [ ] **Phase 13 — Observability + onboarding.** Structured logging; optional Sentry behind env; cost dashboard from `llm_runs`; in-app eval/metrics view; first-run onboarding (connect GitHub, define first ICP, seed sample signals); empty states. **Checkpoint:** a brand-new account is guided to a populated feed in a few clicks.
- [ ] **Phase 14 — Deploy + docs + legal.** Dockerfiles (app + worker); deploy config (Vercel/Fly + separate worker + Neon/Supabase); health checks; migrations on deploy; thorough README; privacy/terms/data-removal stubs (flag GDPR/CCPA); final `typecheck && test && eval`. **Checkpoint:** fresh clone deploys from README; all gates green.

---

## Assumptions / Blockers / Decisions

- **2026-06-12 — Project location:** Built in its own repo `/Users/sahielbose/Signal-Scout` (remote `github.com/sahielbose/Signal-Scout`), fully separate from any other project per the kickoff instruction.
- **2026-06-12 — Flat single-package, not a pnpm workspace.** BUILD_PLAN §2 says "keep it flat for a 1-day build"; one `package.json`, one `tsconfig`, shared `node_modules`. `worker/` and `mcp/` run via `tsx`. Simpler typecheck/install; revisit if it ever needs independent deploys.
- **2026-06-12 — Local DB port 5434** (not 5432/5433) to avoid colliding with other local Postgres containers on this machine.
- **2026-06-12 — shadcn primitives hand-rolled** (`components/ui/*`) instead of the interactive `shadcn` CLI, to keep the build non-interactive/deterministic. Same Radix + cva + Tailwind-variable approach.
- **2026-06-12 — Embeddings:** if no remote embedder key is set, fall back to a deterministic local hash-based embedding so the prefilter still functions offline (flagged TODO to swap for a real embedder). Keeps the build runnable with zero keys.
- **2026-06-12 — Model IDs:** classify defaults to `claude-haiku-4-5-20251001` (cheap), research to `claude-sonnet-4-6`. Overridable via env. With no `ANTHROPIC_API_KEY`, LLM calls use a deterministic mock so the pipeline + evals run offline (flagged).
